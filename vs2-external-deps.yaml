# Controller only. Scientific tool/model dependencies remain in
# virsorter/envs/vs2.yaml and are installed by the native Snakemake rules.
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - conda>=24.7
  - pip:
      - snakemake>=9.23.1,<10
      - click>=8
      - ruamel.yaml>=0.18
